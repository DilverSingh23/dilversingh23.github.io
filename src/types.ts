export interface Experience {
    companyName: string,
    timeWorked: string,
    bullets: string[],
    imgSrc: string,
    headerColor: string
}

export interface Education {
    collegeName: string,
    degree: string,
    graduationInfo: string,
    bullets: string[]
    imgSrc: string
}
